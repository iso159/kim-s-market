import React, { Component } from 'react';
import PublicRoute from '../router';

class App extends Component {
    render() {
        return (
            <PublicRoute />
        );
    }
}

export default App;